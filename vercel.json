{
  "version": 2,
  "framework": null,
  "buildCommand": "cd character-generator && npm install && npm run build && cd ../frame-motion-tool && npm install && npm run build",
  "outputDirectory": "."
}
